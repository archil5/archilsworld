export interface CloudPattern {
  id: string;
  title: string;
  icon: string;
  category: "integration" | "resilience" | "data" | "migration" | "deployment";
  oneLiner: string;
  problem: string;
  solution: string;
  howItWorks: string[];
  tradeoffs: { pro: string; con: string }[];
  realWorld: string;
  whenToUse: string[];
  whenNotToUse: string[];
  relatedPatterns: string[];
}

export const CLOUD_PATTERN_CATEGORIES = [
  { value: "integration" as const, label: "Integration" },
  { value: "resilience" as const, label: "Resilience" },
  { value: "data" as const, label: "Data" },
  { value: "migration" as const, label: "Migration" },
  { value: "deployment" as const, label: "Deployment" },
];

export const CLOUD_PATTERNS: CloudPattern[] = [
  {
    id: "event-sourcing",
    title: "Event Sourcing",
    icon: "📜",
    category: "data",
    oneLiner: "Store every state change as an immutable event, not just the current state.",
    problem: "You need a complete audit trail, the ability to replay history, or to reconstruct state at any point in time. Traditional CRUD overwrites data and loses context.",
    solution: "Instead of storing the current state, store every event that led to it. The current state is derived by replaying events. Think of it like a bank ledger — you don't store your balance, you store every transaction.",
    howItWorks: [
      "Every state change is captured as an immutable event (e.g., OrderPlaced, ItemAdded, PaymentReceived)",
      "Events are appended to an event store — never updated or deleted",
      "Current state is rebuilt by replaying events from the beginning (or from a snapshot)",
      "Projections read events and build read-optimized views for queries",
      "Snapshots periodically capture materialized state to avoid replaying millions of events",
    ],
    tradeoffs: [
      { pro: "Complete audit trail — you can answer 'what happened and when' for any entity", con: "Event store grows unbounded — requires snapshot strategy and eventual archival" },
      { pro: "Time travel — reconstruct state at any historical point", con: "Event schema evolution is genuinely hard — versioning events requires discipline" },
      { pro: "Natural fit for event-driven architectures and CQRS", con: "Steep learning curve — teams used to CRUD will struggle initially" },
    ],
    realWorld: "At BMO, we used event sourcing for the AI platform's model lifecycle — every model version, evaluation, deployment, and rollback was an immutable event. When regulators asked 'why was this model promoted to production?', we could replay the exact chain of events that led to that decision.",
    whenToUse: ["Regulatory/compliance environments that need audit trails", "Systems where you need to 'undo' or replay operations", "Complex domains where understanding *why* state changed matters", "Event-driven microservices architectures"],
    whenNotToUse: ["Simple CRUD apps with no audit requirements", "High-throughput systems where event replay latency is unacceptable", "Teams without experience in event-driven thinking"],
    relatedPatterns: ["CQRS", "Saga Orchestration", "Event-Driven Architecture"],
  },
  {
    id: "cqrs",
    title: "CQRS",
    icon: "⚖️",
    category: "data",
    oneLiner: "Separate read and write models — they have different scaling needs.",
    problem: "Your read and write workloads have fundamentally different shapes. Writes need strong consistency and validation. Reads need speed and flexible queries. One model can't optimize for both.",
    solution: "Command Query Responsibility Segregation splits your system into a write side (commands) and a read side (queries). Each has its own model, its own store, and scales independently.",
    howItWorks: [
      "Commands (writes) go through validation, business rules, and are persisted to the write store",
      "Events are published when commands succeed",
      "Read-side projections consume events and build denormalized, query-optimized views",
      "Queries hit the read store directly — no joins, no complex queries, just fast lookups",
      "Read and write stores can use completely different databases (e.g., Postgres for writes, DynamoDB for reads)",
    ],
    tradeoffs: [
      { pro: "Independent scaling — read-heavy systems can scale reads without touching writes", con: "Eventual consistency between read and write sides — UI must handle stale data" },
      { pro: "Read models are perfectly shaped for each query — no N+1 problems", con: "More infrastructure to manage — two stores, event bus, projections" },
      { pro: "Write side stays simple — focused on business logic only", con: "Debugging is harder — 'where did this data come from?' requires tracing through events" },
    ],
    realWorld: "In the API Gateway platform, we separated command processing (API registration, policy updates) from query serving (developer portal, API catalog). The write side enforced complex validation rules while the read side served the catalog from a denormalized DynamoDB table with sub-10ms latency.",
    whenToUse: ["Read/write ratio is heavily skewed (90%+ reads)", "Read and write models need different database technologies", "Combined with Event Sourcing for full audit + performance"],
    whenNotToUse: ["Simple domains where a single model works fine", "Teams that can't tolerate eventual consistency in the UI", "Low-traffic systems where the complexity isn't justified"],
    relatedPatterns: ["Event Sourcing", "Materialized View", "Event-Driven Architecture"],
  },
  {
    id: "saga-orchestration",
    title: "Saga Orchestration",
    icon: "🎭",
    category: "integration",
    oneLiner: "Coordinate distributed transactions across services with compensating actions.",
    problem: "You have a business process that spans multiple microservices. Traditional database transactions don't work across service boundaries. If step 3 fails, you need to undo steps 1 and 2.",
    solution: "A saga breaks a distributed transaction into a sequence of local transactions. Each step either succeeds and triggers the next, or fails and triggers compensating actions to undo previous steps. An orchestrator coordinates the entire flow.",
    howItWorks: [
      "An orchestrator (e.g., Step Functions, Temporal) manages the saga lifecycle",
      "Each step is a local transaction in a single service",
      "On success, the orchestrator triggers the next step",
      "On failure, compensating transactions run in reverse order to undo completed steps",
      "The orchestrator maintains saga state and handles retries, timeouts, and dead letters",
    ],
    tradeoffs: [
      { pro: "Maintains data consistency across distributed services without distributed transactions", con: "Compensating actions are hard to get right — 'un-sending' an email is impossible" },
      { pro: "Orchestrator provides clear visibility into where a process is and what failed", con: "Single point of failure — if the orchestrator goes down, all in-flight sagas stall" },
      { pro: "Natural fit for AWS Step Functions, Azure Durable Functions, or Temporal", con: "Debugging complex sagas with many branches and compensations is painful" },
    ],
    realWorld: "For the MLOps pipeline, model deployment was a saga: validate model → register in model registry → deploy to staging → run evaluation → promote to production. If evaluation failed, compensating actions rolled back the staging deployment and marked the model as 'failed evaluation' in the registry.",
    whenToUse: ["Multi-service business processes that need consistency guarantees", "Long-running workflows (hours/days) with human approval steps", "Processes where you need clear visibility into execution state"],
    whenNotToUse: ["Within a single service — just use a database transaction", "When compensating actions are impossible or extremely complex", "Simple fire-and-forget event flows (use choreography instead)"],
    relatedPatterns: ["Event Sourcing", "Circuit Breaker", "Outbox Pattern"],
  },
  {
    id: "circuit-breaker",
    title: "Circuit Breaker",
    icon: "⚡",
    category: "resilience",
    oneLiner: "Stop calling a failing service — fail fast instead of cascading failures.",
    problem: "A downstream service is slow or down. Every request to it times out after 30 seconds, consuming threads, connections, and memory. Your service becomes unresponsive too. The failure cascades upstream.",
    solution: "A circuit breaker monitors call success/failure rates. When failures exceed a threshold, the circuit 'opens' and immediately rejects calls without attempting them. After a cooldown period, it lets a few test calls through ('half-open'). If they succeed, the circuit closes and normal flow resumes.",
    howItWorks: [
      "CLOSED state: calls flow normally. Failures are counted.",
      "When failure rate exceeds threshold (e.g., 50% of last 20 calls), circuit OPENS",
      "OPEN state: all calls fail immediately with a fallback response. No network call made.",
      "After a timeout (e.g., 30s), circuit moves to HALF-OPEN — allows 1-3 test calls through",
      "If test calls succeed, circuit CLOSES. If they fail, circuit OPENS again.",
    ],
    tradeoffs: [
      { pro: "Prevents cascade failures — your service stays responsive even when dependencies are down", con: "Fallback responses may not be acceptable for all use cases" },
      { pro: "Gives failing services time to recover instead of being hammered with retries", con: "Threshold tuning is tricky — too sensitive causes false opens, too lenient defeats the purpose" },
      { pro: "Pairs well with bulkheads and rate limiting for defense-in-depth", con: "Half-open state logic can be complex — especially with concurrent requests" },
    ],
    realWorld: "Every external API call in our container platform went through a circuit breaker. When a third-party identity provider went down during peak hours, the circuit opened within 5 seconds. We served cached authorization decisions from a local DynamoDB table while the provider recovered. Users never noticed.",
    whenToUse: ["Any call to an external service or database", "Services with known reliability issues", "When you have meaningful fallback responses (cached data, defaults, degraded mode)"],
    whenNotToUse: ["Internal method calls within the same process", "When there's no meaningful fallback — failing fast without an alternative isn't useful", "Fire-and-forget async messaging (the queue handles backpressure)"],
    relatedPatterns: ["Bulkhead", "Retry with Backoff", "Timeout Pattern"],
  },
  {
    id: "strangler-fig",
    title: "Strangler Fig",
    icon: "🌿",
    category: "migration",
    oneLiner: "Incrementally replace a legacy system by routing traffic to the new one, piece by piece.",
    problem: "You have a monolith that needs to be replaced, but a big-bang rewrite is too risky. The business can't afford downtime, and the monolith handles 200+ features.",
    solution: "Named after the strangler fig tree that grows around a host tree and eventually replaces it. You build new features in a new system, then incrementally redirect traffic from the old to the new using a routing facade. The monolith shrinks over time until it can be decommissioned.",
    howItWorks: [
      "Place a routing layer (API gateway, reverse proxy) in front of the monolith",
      "Build each new feature or migrated capability as a microservice",
      "Route traffic for that feature from the monolith to the new service",
      "The monolith continues serving everything else unchanged",
      "Repeat until the monolith has no remaining traffic and can be decommissioned",
    ],
    tradeoffs: [
      { pro: "Zero big-bang risk — each migration is small and reversible", con: "Two systems running in parallel increases operational complexity and cost" },
      { pro: "Business continuity — the monolith keeps working throughout", con: "Data synchronization between old and new systems is the hardest part" },
      { pro: "Team can learn microservices incrementally on real production traffic", con: "Can take years — requires sustained organizational commitment" },
    ],
    realWorld: "At BMO, migrating from on-prem API management to AWS API Gateway was a textbook strangler fig. We placed an NLB in front of both systems, migrated APIs one team at a time over 8 months, and decommissioned the legacy gateway with zero downtime.",
    whenToUse: ["Legacy monolith modernization", "When you can't afford a big-bang migration", "When different parts of the monolith can be migrated independently"],
    whenNotToUse: ["Greenfield projects — just build it right", "Tightly coupled monoliths where features can't be cleanly separated", "When the monolith is being retired entirely (just sunset it)"],
    relatedPatterns: ["Anti-Corruption Layer", "Branch by Abstraction", "Facade Pattern"],
  },
  {
    id: "sidecar",
    title: "Sidecar / Service Mesh",
    icon: "🏎️",
    category: "deployment",
    oneLiner: "Attach infrastructure concerns to your service as a co-deployed proxy.",
    problem: "Every microservice needs the same cross-cutting concerns: mTLS, logging, tracing, rate limiting, retries. Implementing these in every service's code creates duplication and inconsistency.",
    solution: "Deploy a sidecar proxy alongside each service instance. The sidecar handles all network traffic in and out of the service, transparently adding mTLS, observability, retries, and traffic shaping. The service code stays focused on business logic.",
    howItWorks: [
      "A sidecar container (e.g., Envoy) is deployed alongside each service pod/task",
      "All inbound and outbound traffic is routed through the sidecar via iptables rules",
      "The sidecar handles mTLS termination, certificate rotation, and identity verification",
      "Observability data (metrics, traces, logs) is collected by the sidecar without app changes",
      "A control plane (e.g., Istio, Linkerd, App Mesh) configures all sidecars centrally",
    ],
    tradeoffs: [
      { pro: "Zero application code changes — services are unaware of the mesh", con: "Latency overhead — every call traverses two proxies (sender sidecar → receiver sidecar)" },
      { pro: "Consistent security and observability across all services", con: "Operational complexity — the mesh itself needs monitoring, upgrades, and debugging" },
      { pro: "Traffic management (canary, blue-green, fault injection) without code changes", con: "Resource overhead — each sidecar consumes CPU and memory" },
    ],
    realWorld: "In the ECS Fargate platform, we deployed Envoy sidecars for mTLS between all services. This gave us defense-in-depth — even if an attacker breached the VPC, they couldn't intercept inter-service traffic. Certificate rotation was automatic, and we got distributed tracing for free.",
    whenToUse: ["50+ microservices where consistent networking is essential", "Zero-trust environments requiring mTLS everywhere", "When you need traffic management without redeploying services"],
    whenNotToUse: ["Small deployments (< 10 services) — overhead isn't justified", "Monolithic applications — there's no inter-service traffic to manage", "Teams without Kubernetes/ECS experience to operate the mesh"],
    relatedPatterns: ["Circuit Breaker", "Ambassador Pattern", "Bulkhead"],
  },
  {
    id: "event-driven",
    title: "Event-Driven Architecture",
    icon: "📡",
    category: "integration",
    oneLiner: "Decouple services by communicating through events, not direct calls.",
    problem: "Service A calls Service B, which calls Service C. If C is slow, A is slow. If B changes its API, A breaks. Direct coupling creates fragile, hard-to-scale systems.",
    solution: "Services publish events to a broker (EventBridge, SNS/SQS, Kafka). Other services subscribe to events they care about. Publishers don't know or care who's listening. This creates loose coupling and enables independent scaling.",
    howItWorks: [
      "A service performs an action and publishes an event (e.g., 'OrderPlaced')",
      "The event is sent to a broker (EventBridge, Kafka, SNS)",
      "Interested services subscribe to specific event types",
      "Each subscriber processes the event independently and at its own pace",
      "If a subscriber is down, events are queued and delivered when it recovers (with DLQ for failures)",
    ],
    tradeoffs: [
      { pro: "Loose coupling — services can be deployed, scaled, and evolved independently", con: "Debugging distributed flows is hard — 'what triggered this?' requires correlation IDs and tracing" },
      { pro: "Natural scalability — producers and consumers scale independently", con: "Eventual consistency — events may arrive out of order or be delayed" },
      { pro: "New consumers can be added without changing producers", con: "Event schema management becomes critical — one bad schema change breaks all consumers" },
    ],
    realWorld: "The CI/CD runner platform was entirely event-driven. GitHub webhook → EventBridge → Lambda orchestrator → ECS task launch → completion event → cleanup Lambda. Each component was independently deployable and scalable. During peak hours, we processed thousands of builds without any component blocking another.",
    whenToUse: ["Microservices that need loose coupling", "When different parts of the system scale at different rates", "Workflows where multiple services need to react to the same event"],
    whenNotToUse: ["When you need synchronous request-response (use API calls)", "Simple two-service integrations where an HTTP call is fine", "When strong consistency is required between services"],
    relatedPatterns: ["Saga Orchestration", "Event Sourcing", "CQRS"],
  },
  {
    id: "bulkhead",
    title: "Bulkhead",
    icon: "🚢",
    category: "resilience",
    oneLiner: "Isolate failures by partitioning resources — like watertight compartments in a ship.",
    problem: "All your API calls share the same connection pool. When one downstream service hangs, it exhausts the pool and every other API call fails too. One bad dependency sinks the entire ship.",
    solution: "Partition resources (thread pools, connection pools, compute) per dependency or per workload. If one partition fills up, it can't starve the others.",
    howItWorks: [
      "Assign separate thread/connection pools per downstream dependency",
      "Each pool has a fixed maximum — when full, new calls are rejected immediately",
      "Critical paths get larger pools than non-critical ones",
      "Monitor pool utilization to detect when a dependency is degrading",
      "Combine with circuit breakers for defense-in-depth",
    ],
    tradeoffs: [
      { pro: "Blast radius containment — one failing dependency can't take down everything", con: "Resource inefficiency — dedicated pools mean some capacity sits idle" },
      { pro: "Predictable failure behavior — you know exactly which dependency is impacted", con: "Tuning pool sizes requires production data — wrong sizes cause false rejections" },
      { pro: "Forces you to think about dependency criticality and failure modes", con: "More complex to implement and monitor than shared pools" },
    ],
    realWorld: "In the API Gateway, we ran separate ECS task groups per API category (payments, identity, data). When the payments API team deployed a memory leak, only their tasks were affected. Identity and data APIs continued serving at full capacity.",
    whenToUse: ["Multi-tenant systems where one tenant shouldn't affect others", "Services with multiple downstream dependencies of varying reliability", "Mission-critical systems where partial availability is better than total failure"],
    whenNotToUse: ["Simple services with one or two dependencies", "When resource constraints make dedicated pools impractical", "Stateless serverless functions (the platform handles isolation)"],
    relatedPatterns: ["Circuit Breaker", "Sidecar / Service Mesh", "Rate Limiting"],
  },
  {
    id: "blue-green",
    title: "Blue-Green Deployment",
    icon: "🔵",
    category: "deployment",
    oneLiner: "Run two identical environments — swap traffic instantly for zero-downtime deploys.",
    problem: "Deploying a new version means downtime. Rolling updates can leave the system in a mixed state. If the new version has a critical bug, rollback takes minutes — each minute costing money.",
    solution: "Maintain two identical production environments: Blue (current) and Green (new). Deploy the new version to Green. Test it. Then swap the load balancer to point to Green. If anything goes wrong, swap back to Blue in seconds.",
    howItWorks: [
      "Blue environment serves all production traffic",
      "Deploy the new version to the Green environment (identical infrastructure)",
      "Run smoke tests and health checks against Green",
      "Switch the load balancer / DNS to point to Green",
      "Blue becomes the standby — ready for instant rollback",
    ],
    tradeoffs: [
      { pro: "Zero-downtime deployments with instant rollback capability", con: "Double the infrastructure cost — two full environments running simultaneously" },
      { pro: "Full confidence in the new version before it takes traffic", con: "Database migrations must be backwards-compatible (both versions hit the same DB)" },
      { pro: "Simple to understand and implement — no complex orchestration", con: "Doesn't help with gradual rollouts or canary testing" },
    ],
    realWorld: "Every ECS Fargate deployment in our platform used blue-green via CodeDeploy. The ALB maintained two target groups. New task definitions were deployed to the green target group, health checks ran for 5 minutes, then traffic shifted. We had a 15-minute automatic rollback window if CloudWatch alarms fired.",
    whenToUse: ["Mission-critical services where downtime is unacceptable", "When you need instant rollback capability", "Stateless services that don't depend on local storage"],
    whenNotToUse: ["When infrastructure cost is a primary concern", "Services with complex local state that can't be replicated", "When you need gradual traffic shifting (use canary instead)"],
    relatedPatterns: ["Canary Deployment", "Feature Flags", "Strangler Fig"],
  },
  {
    id: "outbox",
    title: "Transactional Outbox",
    icon: "📤",
    category: "integration",
    oneLiner: "Guarantee event delivery by writing events to the database in the same transaction as state changes.",
    problem: "You update a database row AND publish an event. If the event publish fails after the DB commit, your system is inconsistent. If you publish first and the DB fails, same problem. Two-phase commit across DB and message broker is impractical.",
    solution: "Write the event to an 'outbox' table in the same database transaction as the state change. A separate process (poller or CDC) reads the outbox and publishes events to the broker. Both the state change and the event record succeed or fail together.",
    howItWorks: [
      "Business logic writes to domain tables AND inserts a row into the outbox table — same transaction",
      "Transaction commits atomically — both writes succeed or both fail",
      "A separate publisher process polls the outbox table for unpublished events",
      "Publisher sends each event to the message broker and marks it as published",
      "Alternative: use CDC (Change Data Capture) to stream outbox changes directly to the broker",
    ],
    tradeoffs: [
      { pro: "Guaranteed at-least-once delivery — if the DB commit succeeded, the event will be published", con: "At-least-once means consumers must be idempotent — they may receive duplicates" },
      { pro: "No distributed transactions or two-phase commit needed", con: "Adds latency — events aren't published in real-time (polling interval)" },
      { pro: "Works with any relational database and any message broker", con: "Outbox table needs cleanup/archival to prevent unbounded growth" },
    ],
    realWorld: "In the model registry, when a model was promoted to production, we wrote the promotion record and a 'ModelPromoted' event to the outbox in one Postgres transaction. A Lambda poller (running every 5 seconds) published events to EventBridge. Downstream consumers (monitoring, alerting, audit) were guaranteed to receive every promotion event.",
    whenToUse: ["Whenever you need to update state AND notify other services reliably", "Systems where eventual consistency is acceptable but missed events are not", "Microservices without access to distributed transaction coordinators"],
    whenNotToUse: ["When real-time event delivery is required (use CDC instead of polling)", "Simple systems where losing an occasional event is acceptable", "When using an event store (Event Sourcing already solves this)"],
    relatedPatterns: ["Event Sourcing", "CDC (Change Data Capture)", "Saga Orchestration"],
  },
];
